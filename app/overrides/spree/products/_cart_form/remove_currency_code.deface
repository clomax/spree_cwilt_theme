replace "div#product-price"
text    "<div id=\"product-price\">
        <span class=\"price selling\">
          <%= Spree::Money.new(@product.variants.last.price, currency: current_currency, with_currency: false).to_s %>
        </span>

        <% if @product.master.can_supply? %>
          <link itemprop=\"availability\" href=\"http://scheme.org/InStock\" />
        <% elsif @product.variants.empty? %>
          <br/>
          <span class=\"out-of-stock\"><%= Spree.t(:out_of_stock) %></span>
        <% end %>
        </div>"

